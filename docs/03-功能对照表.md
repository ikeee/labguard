---
日期: 2026-09-23
类型: 对照
状态: 草稿
标签: [机房管理, 复刻, 功能对照]
---

# 原版 ↔ 复刻版 功能对照表

原版信息来自 `docs/01` 的字符串还原与 MSI 分析；复刻版信息来自源码（`src/`）。

| # | 原版行为（证据） | 原版实现 | 复刻版实现 | 默认开关 |
|---|---|---|---|---|
| 1 | "电子教室程序被暂停！" | `NtSuspendProcess/NtResumeProcess` | `ClassroomGuard` 轮询线程 `WaitReason.Suspended` → `NtResumeProcess` | 开 |
| 2 | "电子教室进程未找到或本地连接IP地址错误！" | 路径+进程名探测（`dzjs`） | 进程名列表 + 路径自动探测 + 手动指定 | 开（提示） |
| 3 | "极域电子教室的2个重要服务不存在/停止！" | `ServiceController` | `Classroom.RequiredServices` 检测并启动 | 开 |
| 4 | "你修改了极域电子教室的参数！" | `SOFTWARE\TopDomain\e-Learning Class\Student` 的 ChannelId/AutoLogin | 检测非法 ChannelId 并回写 | 开 |
| 5 | "你拔了网线或禁用了网络连接！"+"请马上恢复网络，否则会全屏锁定！" | 轮询 IP，`169.254.` 判定；开机 2 分钟后检测 | `NetworkGuard.IsDisconnected()`：网卡 Down／IP 清空／`0.0.0.0`／`169.254.*`，持续 20 秒确认；`StartDelaySeconds=120` 预热 | 开（提示） |
| 5b | 原版锁定后**只能重启/输密码**（官方 FAQ 里一堆"电脑死机怎么办"） | 全屏锁定 + `BlockInput`，无自愈 | **断网遮罩**：随机壁纸背景 + 大字提示 + 机位号 + 倒计时，吞键但**插回网线自动消失**；老师 5×Esc + 密码为出口（解除即暂停监控）；可选响鸣（默认 60 秒后 3 次） | 遮罩开、响鸣开 |
| 5c | 原版只认"169.254.*"、且把任意网卡断开都当断网（一体机做热点时会误报/漏报） | 无网卡角色概念 | `NicRoles` 判据网卡机制：忽略热点/无线直连/虚拟网卡、识别 ICS 网关 `192.168.137.1`、可选择"全部断开才算断网"，DNS 锁定只作用在判据网卡 | 自动 + 可指定 |
| 6 | "你修改了IP地址！原地址为:" | WMI 事件比对 + 还原 | 启动基线比对 + `netsh` 还原（DHCP/静态） | 开 |
| 7 | "你启用了防火墙…现在助手强制关闭防火墙！" | 读 `FirewallPolicy\*Profile\EnableFirewall` → `netsh advfirewall` | 同左 | 开 |
| 8 | 破解/脱控工具清单（极域杀手、翘课、KillControl…） | 窗口标题+进程名表 | `DefaultBlocklists.ClassroomCrack`（28 条，原样） | 开（结束进程） |
| 9 | 进程/内核工具清单（Process Hacker、PC Hunter…） | 同上 | `ProcessTools`（28 条） | 开 |
| 10 | 虚拟桌面清单（Dexpot、MultiDesk…） | 同上 | `VirtualDesktop`（12 条） | 开 |
| 11 | 杀软/管家（360safe、HipsTray、QQPCTray…） | `shadu_jianche` | `AntiVirus`（16 条） | 开 |
| 12 | 解压软件（winrarrr、7zFMMM…） | 同上 | `Archivers`（12 条） | 开 |
| 13 | 小游戏 + IFEO `Debugger=null` | 进程表 + IFEO | `Games` + `GamesIfeo`（14 键） | 开 |
| 14 | 任务管理器 / 注册表编辑器 / cmd / PowerShell | 窗口标题（gaowei/zcb/cmd_jianche） | 标题拦截 + 组策略键 | 开/开/关 |
| 15 | 任务视图与 Win 键（`ShowTaskViewButton=0`、键盘拦截、`NoTrayContextMenu`） | Explorer 键 + `keybd_event`/`BlockInput` | Explorer 键 + 低级键盘钩子 | 开 |
| 16 | 新 exe/reg/bat/zip… 文件删除 + 三档模式 | `FileSystemWatcher` + 轮询 + `tdfilefilter` | `FileCreationGuard`（watcher + 三档） | 开（仅 C++） |
| 17 | USB 存储禁用（`usbstor` Start=4，4 个 ControlSet） | 注册表 | `UsbGuard`（同左，退出还原 3） | 开（禁止） |
| 18 | hosts 黑名单（`hmd_local.txt` 75 域名 / `hmd_net.txt` 联网下载） | 写 hosts + `flushdns` + 隐藏 + 备份 | `HostsGuard`（内置 75 条 + 自定义）；**默认关闭**，只做 hosts 文件 ACL 只读 | **关**（改用集中 DNS） |
| 18b | —（原版无此能力） | — | **`DnsGuard` 集中 DNS 锁定**：学生机 DNS 被改即 `netsh` 强制改回 + `flushdns` + AdGuard 存活探针 | 开（需填教师机 IP） |
| 18c | 组策略里的浏览器策略 | Chrome/Edge/Firefox/IE 策略 | 追加**关 DoH**：`DnsOverHttpsMode=off`、`BuiltInDnsClientEnabled=0`、`DNSOverHTTPS.Enabled=0`、Win11 `DoHPolicy=0` | 开 |
| 19 | 浏览器下载/另存为/开发者工具/小恐龙/冲浪 | 组策略（Chrome/Edge/Firefox/IE） | `BrowserPolicyGuard`（同键值） | 开 |
| 20 | 假浏览器 + 快捷方式劫持 + 主页导航站 | `zy\*.exe`（冒充 Chrome/Edge…） | `LabGuard.Launcher.exe`（不冒充，主页可配） | **关**（默认不接管） |
| 21 | "带网络连接的安全模式"禁掉 | 删除 `SafeBoot\Network` + `bcdedit displaybootmenu no` | `SafeModeGuard`（删除前导出备份） | 开 |
| 22 | 机房规范壁纸 + 右上角机器编号（计算机名后 6 位） | 6 张 jpg + `bianhao` 窗体 | `WallpaperGuard` 实时合成 + `make-wallpapers.ps1` 自绘 6 张 | 需在设置里选图 |
| 23 | 注册表 ACL（`regini` + `regqx.ini`） | 微软 regini.exe | `RegistryAclGuard`（.NET RegistrySecurity） | 开 |
| 24 | 服务守护 + `sc failure … reboot` | zmserv | `WatchdogGuard` + `sc failure actions= restart`（不改 reboot） | 开（服务重启） |
| 25 | 键鼠锁定 / 全屏锁定 / 蓝屏锁定 | `BlockInput` + 全屏窗体 | `LockScreenForm`（全屏置顶 + 键盘钩子 + 密码解锁） | 按需 |
| 26 | 密码（默认 `111111`，CAPI 哈希，改动即还原） | `Software\jfglzs` 混淆值名 | PBKDF2-SHA256 + DPAPI 配置 + ACL | 必填 |
| 27 | 退出/暂停（托盘"系统功能(F)"）+ 卸载还原 | about.exe / uninstall.exe | 托盘菜单 + `--unlock` 快捷方式 + `LabGuard.Uninstall.exe` | — |
| 28 | 安装到 `c:\f<物理内存><虚拟内存>` | MSI `SET_APPDIR` | `install.ps1` 默认同款路径公式（`-SimplePath` 可改） | — |
| 29 | 要求先禁用 Defender / 卸载杀毒 | 安装说明 | **不做**，改为建议加白名单 | — |
| 30 | 原版设置程序约 12 组开关，但仍有大量行为写死在代码里（黑名单来源、延时、处置动作、拦截清单） | 反编译未见可配置项 | 复刻版 **94 个配置属性 / 80 个开关全部可配**，并加自检强制"无隐藏开关"；支持导出/导入/恢复默认 | 全开 |
