# 更新记录

## 未发布

1. **README 增加「8. 界面预览」**：安装向导 6 页 + 设置程序 14 组 + 断网遮罩共 21 张图入库
   （`docs/screenshots/`）。这些图是**程序自己渲染**的，不是手工截屏，改完界面重跑一次即可重出，
   不会和代码不同步：`LabGuard.Settings.exe --screenshot <目录>` / `--screenshot-mask <文件>`、
   `LabGuard.Installer.exe --screenshot <目录>`（都只渲染，不改系统、不安装）。
2. **修正**：v0.02 那条"详细的自定义开关管理面板"当时只进了**安装向导第 5 步**，
   `LabGuard.Settings.exe` 仍是旧的一列式布局 —— 现在设置程序主体也换成同一套分组面板
   （左侧分组导航 + 搜索 + 只看已改动 + 改动计数）。
3. 托盘"无界面降级模式"状态改为可读属性（消除一个编译警告，行为不变）。
4. **修断网遮罩的"机位号"看不到**：机位号和"已断开时长"原本抢同一个标签，
   一旦打开「遮罩上显示已断开时长」，机位号就会被时长顶掉（等于第 11 组那个开关白设）。
   现在两者拼成一行同时显示：`机位 1001 · 已断开 0 分 42 秒 · 插回网线后 10 秒内自动恢复`。

## v0.02（2026-09-26）

按使用反馈做的 5 项调整：

1. **不再替换学生机的系统壁纸** —— 删除了"统一壁纸/壁纸守护"整套逻辑；
   机位编号移到「11 机位标识」，只显示在程序自己的断网遮罩上，**完全不碰学生的桌面**。
2. **修掉"启动／运行时闪退"**：
   - 所有程序都加了**崩溃兜底**：未处理异常会写 `%ProgramData%\LabGuard\logs\crash-*.log` 并弹框说明原因（不再无声退出）；
   - 没有桌面会话（例如被服务在会话 0 启动、explorer 未运行时）自动**降级为无界面运行**，监控照常、不会崩；
   - 服务只通过"计划任务 + /i"把代理拉进**当前登录用户会话**，不再用 `cmd start` 误落会话 0（这是原来闪退的主因之一）；
   - 安装器项目此前漏加进解决方案，已修正（避免"改了源码但产物没更新"）。
3. **详细的自定义开关管理面板**：左侧**分组导航**（标出每组改了几项）+ 顶部**搜索**（当前分组内按名称/说明过滤）+
   **「只看已改动」**（与出厂默认值比较）+ "本组显示 x/y · 全局已改动 N 项"计数。
4. **自动识别现有课堂软件路径**：安装向导新增第 3 步「课堂软件」，设置里第 1 组也有【自动识别】按钮；
   识别来源=正在运行的进程 → 常见安装路径 → 控制面板卸载记录 → 注册表 App Paths；**识别不到就直接给你填写框/浏览按钮**。
   无人值守也可指定：`--classroom "C:\...\StudentMain.exe"`。
5. **托盘右键菜单**去掉「打开安装目录」；新增开关**「隐藏安装目录」**（默认开：给安装目录加隐藏属性，学生不易发现；
   关掉后老师/维护人员能正常看到，权限不变）。

## v0.01（2026-09-24）首个公开版本

**这是什么**：面向学校计算机机房的**开源学生机管控程序**（独立实现）——
保护课堂软件（极域 / 红蜘蛛 / 锐捷云课堂）不被学生脱离控制，规范机房上机行为（下载、小游戏、
任务管理器/注册表、U 盘、浏览器下载等），并可选配合集中 DNS 过滤。

> 本版本是**公开发布版**：产品名、界面文案、拦截清单与文档全部为自有内容，
> 仓库内不含任何第三方程序的代码、素材或二进制；6 张壁纸由脚本现场绘制。
> 电子教室产品名仅用于说明"与什么配合使用"（客观描述），不代表任何关联或授权关系。

### 组成

| 程序 | 作用 |
|---|---|
| `LabGuard.Service.exe` | 守护服务（SYSTEM）：hosts、USB、注册表权限、安全模式限制、浏览器策略、新文件监控、代理守护 |
| `LabGuard.Agent.exe` | 托盘小助手（用户会话）：电子教室保护、网络/IP/DNS 守护、违规软件拦截、断网遮罩、壁纸编号 |
| `LabGuard.Settings.exe` | 设置程序：**94 个开关**全部可自行开关 + 导出/导入配置 + 恢复默认 |
| `LabGuard.Uninstall.exe` | 卸载（要密码；逐项还原系统） |
| `LabGuard.Launcher.exe` | 浏览器"上网入口"（可选，不冒充其它浏览器） |
| `LabGuard.Installer.exe` | **一键安装向导**：合规确认 → 安装位置 → 密码 → 功能开关 → 安装（单文件，内置程序与壁纸） |
| `LabGuard.SelfTest.exe` | 自检：口令 / hosts / 拦截清单 / 网卡判据 / 设置项覆盖 / 引擎干跑 |

### 本版要点

- **94 个配置项全部可在界面开关**，并有自检强制"无隐藏开关"（含 Get/Set 往返校验）
- **断网处置**：20 秒去抖确认 → 全屏遮罩（随机壁纸 + 机位号 + 已断开时长，**插回网线自动消失**）+ 可选限次响鸣；
  老师连续 5 次 `Esc` + 密码解除
- **集中 DNS 锁定**（配合教师机 AdGuard）：DNS 被改即改回 + 关闭浏览器/系统 DoH
- **网卡判据**：只监视有线网卡（一体机机房"无线常开做热点"不会误判），热点/虚拟网卡自动忽略
- **抗破解**：服务 DACL 去掉普通用户停止权、异常 5 秒自动重启、服务镜像放在受保护副本目录、
  程序文件被删/被改自动恢复、配置文件有注册表加密镜像、心跳上报（可选）
- **老师后路**：安全模式（含无网络）**不加载**任何管控；`scripts/cleanup-all.ps1` 一键彻底卸载还原
  （纯 PowerShell、不依赖安装目录文件、安全模式可用）
- **口令保护**：退出/暂停/改设置/解除遮罩/卸载全部要密码；改密码需旧密码；静默卸载需 `--password`
- **刻意不做**（与不同）：不要求卸载杀毒软件、不改 UAC 设置、不劫持浏览器主页、不冒充其它软件、
  不用"删文件就蓝屏 / 服务异常就重启电脑"这类恐吓式手段

### 澄清：AdGuard（集中 DNS 过滤）是**可选项**

- **默认不需要 AdGuard**：上网管控有三档——① 完全不控（默认）② 本地 hosts 黑名单（纯本机，无需服务器）
  ③ 集中 DNS（可选，配合教师机上的 AdGuard Home）。三档都在设置程序里随时切换。
- 只有选第 ③ 档才需要 AdGuard；不填 DNS 地址时程序不会锁定 DNS，也不会因此报错。
- 安装教程请走 **`LabGuard-Setup-v0.01.exe` 一键安装向导**（README 第 2 节 / docs/04 第 2 节）。

### 已知边界

- 学生拥有**本地管理员**权限时，以上用户态保护都只是"减速带"；根本解是标准账户 + 无盘还原/母盘派发 + 交换机层限制
- 尚未在真实学生机房做完整课堂验证（建议先 1 台试运行一周）
